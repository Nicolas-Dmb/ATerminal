version: '3.8'
services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: aterminal-api
    ports:
     - "8000"
    env_file:
      - ./api/.env
    restart: unless-stopped